# Task List: SME Operational Reports & Health Dashboard

- `[ ]` 1. Khai báo các state mới cho cấu hình và xem chi tiết Tenant trong `PlatformAdmin.tsx`
- `[ ]` 2. Xây dựng Drawer/Modal chi tiết sức khỏe (`.modal-glass`) với đồ thị Recharts
- `[ ]` 3. Nâng cấp giao diện Tab Health: Tích hợp chọn dòng và hiển thị tổng quan tài nguyên
- `[ ]` 4. Nâng cấp giao diện Tab Config: Bổ sung các input cấu hình đo lường SME (Storage, Rate limits, Toggles, Alerts)
- `[ ]` 5. Tích hợp hiển thị logs kiểm định Blockchain và lỗi nhiệm vụ (Work exceptions logs)
- `[ ]` 6. Biên dịch thử nghiệm Vite Client (`docker compose up -d --build frontend`) và sửa lỗi cú pháp nếu có
- `[ ]` 7. Khởi chạy và kiểm thử chức năng trực quan trên trình duyệt (Platform Admin)
- `[ ]` 8. Đồng bộ Git commit & push mã nguồn
